package playground.functions

fun <T, R> List<T>.mapFunction(transform: (T) -> R): List<R> {
    val result = arrayListOf<R>()
    for (item in this)
        result.add(transform(item))
    return result
}